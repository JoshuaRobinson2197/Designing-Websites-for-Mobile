import { StringTypePipePipe } from './string-type-pipe.pipe';

describe('StringTypePipePipe', () => {
  it('create an instance', () => {
    const pipe = new StringTypePipePipe();
    expect(pipe).toBeTruthy();
  });
});
